<%= tag.svg(
      tag.span("", class: "sparkline--tooltip", hidden: true),
      class: svg_classes,
      width: width,
      height: height,
      "stroke-width": stroke_width,
      data: {
        controller: "sparkline-graph",
        values: values
      }
    ) %>
