<%= render(PageComponent.new(subject: @organization, sidebar: true, sidebar_partial: "/organization_pixels/sidebar")) do |component| %>
  <% component.with(:header) do %>
    <%= render(Page::HeaderComponent.new(title: "Edit Conversion Pixel", subtitle: "Fill out the form below")) %>
  <% end %>
  <% component.with(:body) do %>
    <%= render "/organization_pixels/form",
      organization: @organization,
      pixel: @pixel,
      url: organization_pixel_path(@organization, @pixel) %>
  <% end %>
<% end %>
